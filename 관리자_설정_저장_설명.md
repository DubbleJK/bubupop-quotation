# 관리자 설정이 기기/재접속 후에도 유지되도록 (Vercel KV)

관리자 페이지에서 저장한 **로그인 PIN·견적 설정**이 다음 상황에서도 유지되려면, **Vercel KV**에 저장해야 합니다.

- 다시 접속했을 때
- 다른 기기(휴대폰, 다른 PC)에서 접속했을 때

---

## 1. 동작 방식

- **KV를 연결한 경우:** 설정이 Vercel KV(서버)에 저장됩니다. 모든 기기·재접속 시 같은 설정이 적용됩니다.
- **KV를 연결하지 않은 경우:** 지금처럼 **이 기기의 localStorage**에만 저장됩니다. 다른 기기나 캐시 삭제 시 초기화됩니다.

---

## 2. Vercel KV 연결 방법 (한 번만 하면 됨)

1. **패키지 설치 (로컬에서 한 번)**  
   ```bash
   cd C:\Users\USER\quotation-app
   npm install
   ```

2. **Vercel 대시보드에서 KV 스토어 만들기**
   - [vercel.com](https://vercel.com) 로그인 → **견적 계산기 프로젝트(bubupop-quotation)** 선택
   - 상단 **Storage** 탭 클릭 → **Create Database** → **KV (Redis)** 선택
   - 이름 예: `quotation-kv` → **Create**
   - 만들어진 KV 스토어에서 **Connect to Project** → 현재 견적 앱 프로젝트 선택 후 연결

3. **환경 변수 자동 적용**  
   연결하면 `KV_REST_API_URL`, `KV_REST_API_TOKEN` 등이 프로젝트에 자동으로 붙습니다. 별도로 복사해서 넣을 필요 없습니다.

4. **다시 배포**  
   Storage 연결 후 **Deployments**에서 **Redeploy** 한 번 하면, 이후 관리자에서 "저장"할 때마다 KV에 저장되고, 모든 기기에서 같은 설정이 적용됩니다.

---

## 3. PIN 확인

- **처음 한 번** 저장할 때는 PIN 확인 없이 그대로 저장됩니다. (그때 설정한 로그인 PIN이 기준이 됩니다.)
- **이후** 저장할 때는, 현재 화면에 입력된 **로그인 PIN**이 **마지막으로 저장된 설정의 로그인 PIN**과 같아야 저장됩니다. 다르면 "PIN이 일치하지 않습니다"라고 나옵니다.

---

## 요약

| 단계 | 할 일 |
|------|--------|
| 1 | `quotation-app` 폴더에서 `npm install` (로컬) |
| 2 | Vercel → 견적 프로젝트 → **Storage** → **Create Database** → **KV** 생성 후 프로젝트에 연결 |
| 3 | **Redeploy** 한 번 실행 |

이후에는 관리자에서 저장한 PIN·견적 설정이 **재접속·다른 기기**에서도 유지됩니다.
