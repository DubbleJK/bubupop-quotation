# 오늘 수정한 부분 한 번에 적용하는 방법

두 프로젝트(포털, 견적 계산기)에 수정이 있으므로 **각각 해당 폴더에서** 커밋 후 푸시하면 됩니다.

---

## 1. 견적 계산기 (quotation-app) 적용

### 수정된 파일 요약
- **공통/설정:** 로그인 PIN 5683, 스티커 기본 간격 5mm  
  → `src/config/settings.ts`
- **스티커 시트:** 아트지/모조지 A4 500·A3 980, 유포지 A4 950·A3 2100, 투명 A4 950·A3 0  
  → `src/config/rates.ts`
- **명함:** 베이직 수수료 8000원  
  → `src/config/rates.ts`
- **/admin 직접 접속:** PIN 없으면 "로그인 PIN을 입력하세요." 알림 후 첫 화면(/)으로 이동  
  → `src/app/admin/page.tsx`
- (그 외) HOME 버튼·포털 URL, 제품명 괄호 제거, 설정 API/로컬 등  
  → `src/app/page.tsx`, `src/app/quote/page.tsx`, `src/lib/portalUrl.ts`, `src/config/products.ts`, `src/lib/config/localProvider.ts`, `src/app/api/config/route.ts`, `package.json` 등

### 적용 명령어 (PowerShell)

```powershell
cd C:\Users\USER\quotation-app
git add .
git status
git commit -m "기본값 수정(PIN 5683, 간격 5mm, 스티커 단가, 명함 베이직 8000) + /admin PIN 요구 + HOME·포털 URL 등"
git push origin main
```

- 푸시 후 **Vercel 견적 계산기 프로젝트(bubupop-quotation)** 가 자동으로 다시 배포됩니다.

---

## 2. 포털 (bubucalculate-app) 적용

### 수정된 파일 요약
- 북마크/탭 아이콘: `logo.png` 사용  
  → `src/app/layout.tsx`
- 견적 계산기: 같은 창에서 이동, 괄호 문구 삭제  
  → `src/app/page.tsx`
- 키워드/블로그: HOME 캡슐 버튼, 메인(/)으로 이동  
  → `src/app/keyword/page.tsx`, `src/app/blog/page.tsx`

### 적용 명령어 (PowerShell)

```powershell
cd C:\Users\USER\bubucalculate-app
git add .
git status
git commit -m "북마크 아이콘 logo.png, 견적 같은 창 이동·문구 정리, 키워드/블로그 HOME 버튼"
git push origin main
```

- 푸시 후 **Vercel 포털 프로젝트(bubupop-portal)** 가 자동으로 다시 배포됩니다.

---

## 3. 순서와 확인

1. **견적 계산기 먼저**  
   `cd C:\Users\USER\quotation-app` → `git add .` → `git commit` → `git push origin main`
2. **포털**  
   `cd C:\Users\USER\bubucalculate-app` → `git add .` → `git commit` → `git push origin main`
3. **Vercel**  
   각 프로젝트 **Deployments** 탭에서 배포가 끝날 때까지 확인 후, 실제 주소에서 동작만 한 번씩 확인하면 됩니다.

---

## 4. 요약 표

| 프로젝트       | 이동 경로              | 푸시 후 배포되는 곳      |
|----------------|------------------------|---------------------------|
| 견적 계산기    | `C:\Users\USER\quotation-app`    | bubupop-quotation (Vercel) |
| 포털           | `C:\Users\USER\bubucalculate-app` | bubupop-portal (Vercel)    |

두 저장소가 다르므로 **각 폴더에서 한 번씩** 위 명령을 실행하면, 오늘 수정한 내용이 한 번에 적용됩니다.
